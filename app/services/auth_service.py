"""Authentication business logic — signup, login, token management."""

from app.core.logging import get_logger

logger = get_logger(__name__)


class AuthService:
    """Handles user signup, login, password hashing, and token issuance."""

    # Implementation will be added when we build the auth feature.
    pass
