"""Aggregation service — combine agent results for the dashboard."""

from app.core.logging import get_logger

logger = get_logger(__name__)


class AggregationService:
    """Combines outputs from all agent services into a unified dashboard view."""

    # Implementation will be added when we build the dashboard feature.
    pass
